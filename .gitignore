# MCP config — contains secrets, never push
.mcp.json

# Environment variables — NEVER push API keys
.env
.env.local
.env.production

# System files
.DS_Store
Thumbs.db
desktop.ini

# Node modules (large, not needed on GitHub)
node_modules/

# Claude Code local settings (permissions, MCP config)
.claude/settings.local.json

# Keep .claude/rules/ and .claude/commands/ tracked (ECC framework)
# They are NOT ignored — only settings.local.json is

data/past_year_papers/**/*.pdf
