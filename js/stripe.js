/**
 * stripe.js
 * Stripe Checkout redirect logic.
 * Calls api/checkout.js with the selected price ID and
 * redirects the user to Stripe's hosted checkout page.
 * PLACEHOLDER: Full implementation coming in Task 3.2
 *
 * ⚠️ CONFIGURE: STRIPE_PUBLISHABLE_KEY must be set in .env
 * and in Vercel dashboard. Never use the secret key here.
 */

// TEST: Click a Subscribe button on pages/pricing.html and verify
// it redirects to a Stripe Checkout page in test mode.
