/**
 * supabase-client.js
 * Initialises the Supabase client and exports a single instance
 * used by all other JS files.
 * PLACEHOLDER: Full implementation coming in Task 2.1
 *
 * ⚠️ CONFIGURE: Set SUPABASE_URL and SUPABASE_ANON_KEY in .env
 * and in Vercel dashboard before going to production.
 */

// TEST: import this file, call supabase.from('profiles').select()
// and verify a response is returned without errors.
