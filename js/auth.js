/**
 * auth.js
 * Authentication functions and session management.
 * Functions: signUp, signIn, signOut, getCurrentUser,
 *            getProfile, isTrialActive, canAccessSubject,
 *            checkDailyUsage, incrementDailyUsage
 * Redirects unauthenticated users to pages/login.html
 * PLACEHOLDER: Full implementation coming in Task 2.2
 *
 * ⚠️ CONFIGURE: Ensure Supabase Auth redirect URLs include
 * https://www.superholiclab.com in the Supabase dashboard.
 */

// TEST: Load any protected page while logged out — should
// redirect to pages/login.html automatically.
